# Example environment variables for this project.
# Copy this file to `.env.local` and fill in real values.
# Do NOT commit `.env.local` to source control. It's ignored by .gitignore.

# Example MongoDB connection string (replace placeholders):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

# Example for a local MongoDB instance:
# MONGODB_URI=mongodb://localhost:27017/my_local_db